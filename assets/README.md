Asset placeholders for the static MVP.

- `images/`: future portrait, hero, and page images.
- `icons/`: future exported icon assets if the inline SVG system is replaced.
- `og/`: create the real Open Graph image here later.
- `portfolio/`: future work thumbnails.

Current pages use CSS fallback visuals so missing image files do not break rendering.
